GET STARTED
===========
To start with `networkc`, you just need to install it using `pip`. Execute the following command:

.. code-block:: bash

   pip install git+https://github.com/atsushi-green/networkc
